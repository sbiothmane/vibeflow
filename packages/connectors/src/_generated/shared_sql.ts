// AUTO-GENERATED by packages/connectors/scripts/generate.ts.
// Do not edit by hand. Re-run `bun --filter @vibeflow/connectors generate`.
// Connector: shared_sql — SQL Server
// Source: https://learn.microsoft.com/en-us/connectors/sql/?accept=text/markdown

import { buildConnectorCall, type ActionSpec, type ConnectorCallOptions, type ExpressionOrValue, type TriggerSpec } from "../_runtime.js";

const API_ID = "/providers/Microsoft.PowerApps/apis/shared_sql";
const DEFAULT_CONNECTION_NAME = "shared_sql";

export const SQLServer = {
  /** Connector id (`shared_sql`). */
  connectorId: "shared_sql",
  /** Power Automate apiId for this connector. */
  apiId: API_ID,

  /**
   * Delete row [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * Operation id: `DeleteItem`
   */
  deleteItem(
    parameters: {
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of table
      id?: ExpressionOrValue; // REQUIRED — Row id — string — Unique identifier of the row to delete
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "DeleteItem",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Delete row (V2)
   * Operation id: `DeleteItem_V2`
   */
  deleteItemV2(
    parameters: {
      server?: ExpressionOrValue; // REQUIRED — Server name — string — Name of SQL server
      database?: ExpressionOrValue; // REQUIRED — Database name — string — Database name
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of table
      id?: ExpressionOrValue; // REQUIRED — Row id — string — Unique identifier of the row to delete
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "DeleteItem_V2",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Execute a SQL query [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @returns The outputs of this operation are dynamic.
   * Operation id: `ExecutePassThroughNativeQuery`
   */
  executePassThroughNativeQuery(
    parameters: {
      query?: ExpressionOrValue; // REQUIRED — Query — dynamic — query body
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "ExecutePassThroughNativeQuery",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Execute a SQL query (V2)
   * @returns The outputs of this operation are dynamic.
   * Operation id: `ExecutePassThroughNativeQuery_V2`
   */
  executePassThroughNativeQueryV2(
    parameters: {
      server?: ExpressionOrValue; // REQUIRED — Server name — string — Name of SQL server
      database?: ExpressionOrValue; // REQUIRED — Database name — string — Database name
      query?: ExpressionOrValue; // REQUIRED — Query — dynamic — query body
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "ExecutePassThroughNativeQuery_V2",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Execute stored procedure
   * @returns The outputs of this operation are dynamic.
   * Operation id: `ExecuteProcedure`
   */
  executeProcedure(
    parameters: {
      procedure?: ExpressionOrValue; // REQUIRED — Procedure name — string — Name of stored procedure
      parameters?: ExpressionOrValue; // REQUIRED — Parameters list — dynamic — Input parameters to the stored procedure
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "ExecuteProcedure",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Execute stored procedure (V2)
   * @returns The outputs of this operation are dynamic.
   * Operation id: `ExecuteProcedure_V2`
   */
  executeProcedureV2(
    parameters: {
      server?: ExpressionOrValue; // REQUIRED — Server name — string — Name of SQL server
      database?: ExpressionOrValue; // REQUIRED — Database name — string — Database name
      procedure?: ExpressionOrValue; // REQUIRED — Procedure name — string — Name of stored procedure
      parameters?: ExpressionOrValue; // REQUIRED — Parameters list — dynamic — Input parameters to the stored procedure
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "ExecuteProcedure_V2",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Get row [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @returns The outputs of this operation are dynamic.
   * Operation id: `GetItem`
   */
  getItem(
    parameters: {
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of SQL table
      id?: ExpressionOrValue; // REQUIRED — Row id — string — Unique identifier of the row to retrieve
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetItem",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Get row (V2)
   * @returns The outputs of this operation are dynamic.
   * Operation id: `GetItem_V2`
   */
  getItemV2(
    parameters: {
      server?: ExpressionOrValue; // REQUIRED — Server name — string — Name of SQL server
      database?: ExpressionOrValue; // REQUIRED — Database name — string — Database name
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of SQL table
      id?: ExpressionOrValue; // REQUIRED — Row id — string — Unique identifier of the row to retrieve
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetItem_V2",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Get rows [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @returns The outputs of this operation are dynamic.
   * Operation id: `GetItems`
   */
  getItems(
    parameters: {
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of SQL table
      $apply?: ExpressionOrValue; // optional — Aggregation transformation — string — A sequence of OData aggregation transformations
      $filter?: ExpressionOrValue; // optional — Filter Query — string — An ODATA filter query to restrict the entries returned (e.g. stringColumn eq 'string' OR numberColumn lt 123).
      $orderby?: ExpressionOrValue; // optional — Order By — string — An ODATA orderBy query for specifying the order of entries.
      $skip?: ExpressionOrValue; // optional — Skip Count — integer — The number of entries to skip (default = 0).
      $top?: ExpressionOrValue; // optional — Top Count — integer — Total number of entries to retrieve (default = all).
      $select?: ExpressionOrValue; // optional — Select Query — string — Specific fields to retrieve from entries (default = all).
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetItems",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Get rows (V2)
   * @returns The outputs of this operation are dynamic.
   * Operation id: `GetItems_V2`
   */
  getItemsV2(
    parameters: {
      server?: ExpressionOrValue; // REQUIRED — Server name — string — Name of SQL server
      database?: ExpressionOrValue; // REQUIRED — Database name — string — Database name
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of SQL table
      $apply?: ExpressionOrValue; // optional — Aggregation transformation — string — A sequence of OData aggregation transformations
      $filter?: ExpressionOrValue; // optional — Filter Query — string — An ODATA filter query to restrict the entries returned (e.g. stringColumn eq 'string' OR numberColumn lt 123).
      $orderby?: ExpressionOrValue; // optional — Order By — string — An ODATA orderBy query for specifying the order of entries.
      $skip?: ExpressionOrValue; // optional — Skip Count — integer — The number of entries to skip (default = 0).
      $top?: ExpressionOrValue; // optional — Top Count — integer — Total number of entries to retrieve (default = all).
      $select?: ExpressionOrValue; // optional — Select Query — string — Specific fields to retrieve from entries (default = all).
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetItems_V2",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * When an item is created [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @remarks This is a trigger operation; pass it to flowBuilder.setTrigger().
   * @returns The outputs of this operation are dynamic.
   * Operation id: `GetOnNewItems`
   */
  getOnNewItems(
    parameters: {
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of SQL table
      $filter?: ExpressionOrValue; // optional — Filter Query — string — An ODATA filter query to restrict the entries returned (e.g. stringColumn eq 'string' OR numberColumn lt 123).
      $top?: ExpressionOrValue; // optional — Top Count — integer — Total number of entries to retrieve (default = all).
      $orderby?: ExpressionOrValue; // optional — Order By — string — An ODATA orderBy query for specifying the order of entries.
      $select?: ExpressionOrValue; // optional — Select Query — string — Specific fields to retrieve from entries (default = all).
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): TriggerSpec {
    return buildConnectorCall({
      kind: "trigger",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetOnNewItems",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as TriggerSpec;
  },

  /**
   * When an item is created (V2)
   * @remarks This is a trigger operation; pass it to flowBuilder.setTrigger().
   * @returns The outputs of this operation are dynamic.
   * Operation id: `GetOnNewItems_V2`
   */
  getOnNewItemsV2(
    parameters: {
      server?: ExpressionOrValue; // REQUIRED — Server name — string — Name of SQL server
      database?: ExpressionOrValue; // REQUIRED — Database name — string — Database name
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of SQL table
      $filter?: ExpressionOrValue; // optional — Filter Query — string — An ODATA filter query to restrict the entries returned (e.g. stringColumn eq 'string' OR numberColumn lt 123).
      $top?: ExpressionOrValue; // optional — Top Count — integer — Total number of entries to retrieve (default = all).
      $orderby?: ExpressionOrValue; // optional — Order By — string — An ODATA orderBy query for specifying the order of entries.
      $select?: ExpressionOrValue; // optional — Select Query — string — Specific fields to retrieve from entries (default = all).
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): TriggerSpec {
    return buildConnectorCall({
      kind: "trigger",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetOnNewItems_V2",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as TriggerSpec;
  },

  /**
   * When an item is modified [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @remarks This is a trigger operation; pass it to flowBuilder.setTrigger().
   * @returns The outputs of this operation are dynamic.
   * Operation id: `GetOnUpdatedItems`
   */
  getOnUpdatedItems(
    parameters: {
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of SQL table
      $filter?: ExpressionOrValue; // optional — Filter Query — string — An ODATA filter query to restrict the entries returned (e.g. stringColumn eq 'string' OR numberColumn lt 123).
      $top?: ExpressionOrValue; // optional — Top Count — integer — Total number of entries to retrieve (default = all).
      $orderby?: ExpressionOrValue; // optional — Order By — string — An ODATA orderBy query for specifying the order of entries.
      $select?: ExpressionOrValue; // optional — Select Query — string — Specific fields to retrieve from entries (default = all).
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): TriggerSpec {
    return buildConnectorCall({
      kind: "trigger",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetOnUpdatedItems",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as TriggerSpec;
  },

  /**
   * When an item is modified (V2)
   * @remarks This is a trigger operation; pass it to flowBuilder.setTrigger().
   * @returns The outputs of this operation are dynamic.
   * Operation id: `GetOnUpdatedItems_V2`
   */
  getOnUpdatedItemsV2(
    parameters: {
      server?: ExpressionOrValue; // REQUIRED — Server name — string — Name of SQL server
      database?: ExpressionOrValue; // REQUIRED — Database name — string — Database name
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of SQL table
      $filter?: ExpressionOrValue; // optional — Filter Query — string — An ODATA filter query to restrict the entries returned (e.g. stringColumn eq 'string' OR numberColumn lt 123).
      $top?: ExpressionOrValue; // optional — Top Count — integer — Total number of entries to retrieve (default = all).
      $orderby?: ExpressionOrValue; // optional — Order By — string — An ODATA orderBy query for specifying the order of entries.
      $select?: ExpressionOrValue; // optional — Select Query — string — Specific fields to retrieve from entries (default = all).
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): TriggerSpec {
    return buildConnectorCall({
      kind: "trigger",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetOnUpdatedItems_V2",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as TriggerSpec;
  },

  /**
   * Lists tables exposed by the connection [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @returns Represents a list of tables. - Body - TablesList
   * Operation id: `GetTables`
   */
  getTables(options?: ConnectorCallOptions): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetTables",
      parameters: {},
      options,
    }) as ActionSpec;
  },

  /**
   * Get tables (V2)
   * @returns Represents a list of tables. - Body - TablesList
   * Operation id: `GetTables_V2`
   */
  getTablesV2(
    parameters: {
      server?: ExpressionOrValue; // REQUIRED — Server name — string — Name of SQL server
      database?: ExpressionOrValue; // REQUIRED — Database name — string — Database name
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetTables_V2",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Update row [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @returns The outputs of this operation are dynamic.
   * Operation id: `PatchItem`
   */
  patchItem(
    parameters: {
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of table
      id?: ExpressionOrValue; // REQUIRED — Row id — string — Unique identifier of the row to update
      item?: ExpressionOrValue; // REQUIRED — Row — dynamic — Row with updated values
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "PatchItem",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Update row (V2)
   * @returns The outputs of this operation are dynamic.
   * Operation id: `PatchItem_V2`
   */
  patchItemV2(
    parameters: {
      server?: ExpressionOrValue; // REQUIRED — Server name — string — Name of SQL server
      database?: ExpressionOrValue; // REQUIRED — Database name — string — Database name
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of table
      id?: ExpressionOrValue; // REQUIRED — Row id — string — Unique identifier of the row to update
      item?: ExpressionOrValue; // REQUIRED — Row — dynamic — Row with updated values
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "PatchItem_V2",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Insert row [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @returns The outputs of this operation are dynamic.
   * Operation id: `PostItem`
   */
  postItem(
    parameters: {
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of table
      item?: ExpressionOrValue; // REQUIRED — Row — dynamic — Row to insert into the specified table
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "PostItem",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Insert row (V2)
   * @returns The outputs of this operation are dynamic.
   * Operation id: `PostItem_V2`
   */
  postItemV2(
    parameters: {
      server?: ExpressionOrValue; // REQUIRED — Server name — string — Name of SQL server
      database?: ExpressionOrValue; // REQUIRED — Database name — string — Database name
      table?: ExpressionOrValue; // REQUIRED — Table name — string — Name of table
      item?: ExpressionOrValue; // REQUIRED — Row — dynamic — Row to insert into the specified table
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "PostItem_V2",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Transform data using Power Query
   * @returns The outputs of this operation are dynamic.
   * Operation id: `SqlExecutePassThroughPowerQuery`
   */
  sqlExecutePassThroughPowerQuery(
    parameters: {
      query?: ExpressionOrValue; // optional — mquery — Query Text
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "SqlExecutePassThroughPowerQuery",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },
} as const;
