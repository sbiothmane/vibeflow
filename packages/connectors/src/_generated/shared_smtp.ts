// AUTO-GENERATED by packages/connectors/scripts/generate.ts.
// Do not edit by hand. Re-run `bun --filter @vibeflow/connectors generate`.
// Connector: shared_smtp — SMTP
// Source: https://learn.microsoft.com/en-us/connectors/smtp/?accept=text/markdown

import { buildConnectorCall, type ActionSpec, type ConnectorCallOptions, type ExpressionOrValue, type TriggerSpec } from "../_runtime.js";

const API_ID = "/providers/Microsoft.PowerApps/apis/shared_smtp";
const DEFAULT_CONNECTION_NAME = "shared_smtp";

export const SMTP = {
  /** Connector id (`shared_smtp`). */
  connectorId: "shared_smtp",
  /** Power Automate apiId for this connector. */
  apiId: API_ID,

  /**
   * Send Email [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @returns - response - string
   * Operation id: `SendEmail`
   */
  sendEmail(
    parameters: {
      ContentData?: ExpressionOrValue; // REQUIRED — Content data — string — Content data (base64 encoded for streams and as-is for string)
      ContentType?: ExpressionOrValue; // REQUIRED — Content type — string — Content type
      ContentTransferEncoding?: ExpressionOrValue; // REQUIRED — Content Transfer Encoding — string — Content Transfer Encoding (base64 or none)
      From?: ExpressionOrValue; // optional — email — Email address of sender like sender@domain.com
      To?: ExpressionOrValue; // optional — email — Specify email addresses separated by semicolons like recipient1@domain.com;recipient2@domain.com
      CC?: ExpressionOrValue; // optional — email — Specify email addresses separated by semicolons like recipient1@domain.com;recipient2@domain.com
      Subject?: ExpressionOrValue; // optional — string — Email subject
      Body?: ExpressionOrValue; // optional — string — Email body
      IsHtml?: ExpressionOrValue; // optional — Is HTML — boolean — Send the email as HTML (true/false)
      Bcc?: ExpressionOrValue; // optional — email — Specify email addresses separated by semicolons like recipient1@domain.com;recipient2@domain.com.
      Importance?: ExpressionOrValue; // optional — string — Importance of the email (High, Normal, or Low)
      ReadReceipt?: ExpressionOrValue; // optional — Read Receipt — email — Specify email address for Read receipt
      DeliveryReceipt?: ExpressionOrValue; // optional — Delivery Receipt — email — Specify email address for Delivery receipt
      FileName?: ExpressionOrValue; // optional — File name — string — File name
      ContentId?: ExpressionOrValue; // optional — Content id — string — Content id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "SendEmail",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Send Email [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * Operation id: `SendEmailV2`
   */
  sendEmailV2(
    parameters: {
      ContentData?: ExpressionOrValue; // REQUIRED — Content data — byte — Content data
      ContentType?: ExpressionOrValue; // REQUIRED — Content type — string — Content type
      From?: ExpressionOrValue; // optional — email — Email address of sender like sender@domain.com
      To?: ExpressionOrValue; // optional — email — Specify email addresses separated by semicolons like recipient1@domain.com;recipient2@domain.com
      CC?: ExpressionOrValue; // optional — email — Specify email addresses separated by semicolons like recipient1@domain.com;recipient2@domain.com
      Subject?: ExpressionOrValue; // optional — string — Email subject
      Body?: ExpressionOrValue; // optional — string — Email body
      IsHtml?: ExpressionOrValue; // optional — Is HTML — boolean — Send the email as HTML (true/false)
      Bcc?: ExpressionOrValue; // optional — email — Specify email addresses separated by semicolons like recipient1@domain.com;recipient2@domain.com.
      Importance?: ExpressionOrValue; // optional — string — Importance of the email (High, Normal, or Low)
      ReadReceipt?: ExpressionOrValue; // optional — Read Receipt — email — Specify email address for Read receipt
      DeliveryReceipt?: ExpressionOrValue; // optional — Delivery Receipt — email — Specify email address for Delivery receipt
      FileName?: ExpressionOrValue; // optional — File name — string — File name
      ContentId?: ExpressionOrValue; // optional — Content id — string — Content id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "SendEmailV2",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Send Email (V3)
   * Operation id: `SendEmailV3`
   */
  sendEmailV3(
    parameters: {
      ContentData?: ExpressionOrValue; // REQUIRED — Content data — byte — Content data
      ContentType?: ExpressionOrValue; // REQUIRED — Content type — string — Content type
      From?: ExpressionOrValue; // optional — email — Email address of sender like sender@domain.com
      To?: ExpressionOrValue; // optional — email — Specify email addresses separated by semicolons like recipient1@domain.com;recipient2@domain.com
      CC?: ExpressionOrValue; // optional — email — Specify email addresses separated by semicolons like recipient1@domain.com;recipient2@domain.com
      Subject?: ExpressionOrValue; // optional — string — Email subject
      Body?: ExpressionOrValue; // optional — html — Email body
      Bcc?: ExpressionOrValue; // optional — email — Specify email addresses separated by semicolons like recipient1@domain.com;recipient2@domain.com.
      Importance?: ExpressionOrValue; // optional — string — Importance of the email (High, Normal, or Low)
      ReadReceipt?: ExpressionOrValue; // optional — Read Receipt — email — Specify email address for Read receipt
      DeliveryReceipt?: ExpressionOrValue; // optional — Delivery Receipt — email — Specify email address for Delivery receipt
      FileName?: ExpressionOrValue; // optional — File name — string — File name
      ContentId?: ExpressionOrValue; // optional — Content id — string — Content id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "SendEmailV3",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },
} as const;
