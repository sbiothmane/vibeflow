// AUTO-GENERATED by packages/connectors/scripts/generate.ts.
// Do not edit by hand. Re-run `bun --filter @vibeflow/connectors generate`.
// Connector: shared_formrecognizer — Azure AI Document Intelligence (form recognizer)
// Source: https://learn.microsoft.com/en-us/connectors/formrecognizer/?accept=text/markdown

import { buildConnectorCall, type ActionSpec, type ConnectorCallOptions, type ExpressionOrValue, type TriggerSpec } from "../_runtime.js";

const API_ID = "/providers/Microsoft.PowerApps/apis/shared_formrecognizer";
const DEFAULT_CONNECTION_NAME = "shared_formrecognizer";

export const AzureAIDocumentIntelligenceFormRecognizer = {
  /** Connector id (`shared_formrecognizer`). */
  connectorId: "shared_formrecognizer",
  /** Power Automate apiId for this connector. */
  apiId: API_ID,

  /**
   * Analyze Business Card (deprecated) [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @returns Status and result of the queued analyze operation. - Body - BusinessCardAnalyzeOperationResult
   * Operation id: `AnalyzeBusinessCardAsync`
   */
  analyzeBusinessCardAsync(
    parameters: {
      includeTextDetails?: ExpressionOrValue; // optional — Include Text Details — boolean — Include text lines and element references in the result. Default: True (just for FR Connector).
      pages?: ExpressionOrValue; // optional — Pages — string — The page selection for multi-page PDF and TIFF documents, to extract Invoice information from individual pages and a ran
      locale?: ExpressionOrValue; // optional — Locale — string — Locale of the invoice. Supported locale includes: en-US.
      inputFileContent?: ExpressionOrValue; // optional — Document/Image File Content — binary — A PDF document or image (JPG or PNG) file to analyze.
      inputFileUrl?: ExpressionOrValue; // optional — Document/Image URL — string — Url path for input file. Alternative to Document/Image File Content.
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "AnalyzeBusinessCardAsync",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Analyze Document for Prebuilt or Custom models (v3.x API)
   * @returns Status and result of the analyze operation. - Body - AnalyzeResultOperation_V30
   * Operation id: `AnalyzeDocumentV30`
   */
  analyzeDocumentV30(
    parameters: {
      "api-version"?: ExpressionOrValue; // REQUIRED — Document Intelligence API version — string — Document Intelligence API version.
      modelId?: ExpressionOrValue; // REQUIRED — Model Identifier — string — Prebuilt modelIds: prebuilt-read, prebuilt-layout, prebuilt-document, prebuilt-businessCard, prebuilt-idDocument, prebui
      pages?: ExpressionOrValue; // optional — Pages — string — The page selection for multi-page PDF and TIFF documents, to extract Invoice information from individual pages and a ran
      locale?: ExpressionOrValue; // optional — Locale — string — Locale hint for text recognition and document analysis. Value may contain only the language code (ex. "en", "fr") or BCP
      stringIndexType?: ExpressionOrValue; // optional — String Index Type — string — Method used to compute string offset and length. Supported options: textElements, unicodeCodePoint, utf16CodeUnit.
      features?: ExpressionOrValue; // optional — Optional and Premium features — array — List of optional analysis features ( such as ocrHighResolution, languages, barcodes, formulas, keyValuePairs and styleFo
      inputFileContent?: ExpressionOrValue; // optional — Document/Image File Content — binary — A PDF document or image (JPG, PNG, BMP, TIFF) file to analyze.
      inputFileUrl?: ExpressionOrValue; // optional — Document/Image URL — string — Url path for input file. Alternative to Document/Image File Content.
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "AnalyzeDocumentV30",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Analyze Document for Prebuilt or Custom models (v4.x API)
   * @returns Status and result of the analyze operation. - Body - AnalyzeResultOperation_V30
   * Operation id: `AnalyzeDocumentV40`
   */
  analyzeDocumentV40(
    parameters: {
      "api-version"?: ExpressionOrValue; // REQUIRED — Document Intelligence API version — string — Document Intelligence API version.
      modelId?: ExpressionOrValue; // REQUIRED — Model Identifier — string — Prebuilt modelIds: prebuilt-read, prebuilt-layout, prebuilt-businessCard, prebuilt-idDocument, prebuilt-invoice, prebuil
      inputFileContent?: ExpressionOrValue; // optional — Document/Image File Content — binary — A PDF document or image (JPG, PNG, BMP, TIFF) file to analyze.
      inputFileUrl?: ExpressionOrValue; // optional — Document/Image URL — string — Url path for input file. Alternative to Document/Image File Content.
      pages?: ExpressionOrValue; // optional — Pages — string — The page selection for multi-page PDF and TIFF documents, to extract Invoice information from individual pages and a ran
      locale?: ExpressionOrValue; // optional — Locale — string — Locale hint for text recognition and document analysis. Value may contain only the language code (ex. "en", "fr") or BCP
      stringIndexType?: ExpressionOrValue; // optional — String Index Type — string — Method used to compute string offset and length. Supported options: textElements, unicodeCodePoint, utf16CodeUnit.
      features?: ExpressionOrValue; // optional — Optional and Premium features — array — List of optional analysis features ( such as ocrHighResolution, languages, barcodes, formulas, keyValuePairs, styleFont 
      queryFields?: ExpressionOrValue; // optional — Query Fields — array — List of additional fields to extract. Ex. "NumberOfGuests,StoreNumber"
      outputContentFormat?: ExpressionOrValue; // optional — Output Content Format — string — Format of the analyze result top-level content.
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "AnalyzeDocumentV40",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Analyze ID Document (deprecated) [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @returns Status and result of the queued analyze operation. - Body - IdAnalyzeOperationResult
   * Operation id: `AnalyzeIdDocumentAsync`
   */
  analyzeIdDocumentAsync(
    parameters: {
      includeTextDetails?: ExpressionOrValue; // optional — Include Text Details — boolean — Include text lines and element references in the result. Default: True (just for FR Connector).
      pages?: ExpressionOrValue; // optional — Pages — string — The page selection for multi-page PDF and TIFF documents, to extract Invoice information from individual pages and a ran
      inputFileContent?: ExpressionOrValue; // optional — Document/Image File Content — binary — A PDF document or image (JPG or PNG) file to analyze.
      inputFileUrl?: ExpressionOrValue; // optional — Document/Image URL — string — Url path for input file. Alternative to Document/Image File Content.
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "AnalyzeIdDocumentAsync",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Analyze Invoice (deprecated) [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @returns Status and result of the queued analyze operation. - Body - InvoiceAnalyzeOperationResult
   * Operation id: `AnalyzeInvoice`
   */
  analyzeInvoice(
    parameters: {
      includeTextDetails?: ExpressionOrValue; // optional — Include Text Details — boolean — Include text lines and element references in the result. Default: True (just for FR Connector).
      pages?: ExpressionOrValue; // optional — Pages — string — The page selection for multi-page PDF and TIFF documents, to extract Invoice information from individual pages and a ran
      locale?: ExpressionOrValue; // optional — Locale — string — Locale of the invoice. Supported locale includes: en-US.
      inputFileContent?: ExpressionOrValue; // optional — Document/Image File Content — binary — A PDF document or image (JPG or PNG) file to analyze.
      inputFileUrl?: ExpressionOrValue; // optional — Document/Image URL — string — Url path for input file. Alternative to Document/Image File Content.
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "AnalyzeInvoice",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Analyze Layout (deprecated) [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @returns Status and result of the queued analyze operation. - Body - LayoutAnalyzeOperationResult
   * Operation id: `AnalyzeLayoutAsync`
   */
  analyzeLayoutAsync(
    parameters: {
      pages?: ExpressionOrValue; // optional — Pages — string — The page selection for multi-page PDF and TIFF documents, to extract Invoice information from individual pages and a ran
      language?: ExpressionOrValue; // optional — Language — string — The BCP-47 language code of the text in the document.
      readingOrder?: ExpressionOrValue; // optional — Reading Order — string — Optional parameter to specify which reading order algorithm should be applied when ordering the extract text elements. C
      inputFileContent?: ExpressionOrValue; // optional — Document/Image File Content — binary — A PDF document or image (JPG or PNG) file to analyze.
      inputFileUrl?: ExpressionOrValue; // optional — Document/Image URL — string — Url path for input file. Alternative to Document/Image File Content.
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "AnalyzeLayoutAsync",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Analyze Receipt (deprecated) [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @returns Status and result of the queued analyze operation. - Body - ReceiptAnalyzeOperationResult
   * Operation id: `AnalyzeReceiptAsync`
   */
  analyzeReceiptAsync(
    parameters: {
      includeTextDetails?: ExpressionOrValue; // optional — Include Text Details — boolean — Include text lines and element references in the result. Default: True (just for FR Connector).
      pages?: ExpressionOrValue; // optional — Pages — string — The page selection for multi-page PDF and TIFF documents, to extract Invoice information from individual pages and a ran
      locale?: ExpressionOrValue; // optional — Locale — string — Locale of the invoice. Supported locale includes: en-US.
      inputFileContent?: ExpressionOrValue; // optional — Document/Image File Content — binary — A PDF document or image (JPG or PNG) file to analyze.
      inputFileUrl?: ExpressionOrValue; // optional — Document/Image URL — string — Url path for input file. Alternative to Document/Image File Content.
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "AnalyzeReceiptAsync",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Analyze Custom Form (deprecated) [DEPRECATED]
   * @deprecated Marked deprecated by Microsoft.
   * @returns Status and result of the queued analyze operation. - Body - CustomFormsAnalyzeOperationResult
   * Operation id: `AnalyzeWithCustomForm`
   */
  analyzeWithCustomForm(
    parameters: {
      modelId?: ExpressionOrValue; // REQUIRED — Model Identifier — string — Format - uuid. Model identifier.
      includeTextDetails?: ExpressionOrValue; // optional — Include Text Details — boolean — Include text lines and element references in the result. Default: True (just for FR Connector).
      pages?: ExpressionOrValue; // optional — Pages — string — The page selection for multi-page PDF and TIFF documents, to extract Invoice information from individual pages and a ran
      inputFileContent?: ExpressionOrValue; // optional — Document/Image File Content — binary — A PDF document or image (JPG or PNG) file to analyze.
      inputFileUrl?: ExpressionOrValue; // optional — Document/Image URL — string — Url path for input file. Alternative to Document/Image File Content.
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "AnalyzeWithCustomForm",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Classify document with document classifier (v3.1 API)
   * @returns Status and result of the analyze operation. - Body - AnalyzeResultOperation_V30
   * Operation id: `ClassifyDocumentV31`
   */
  classifyDocumentV31(
    parameters: {
      classifierId?: ExpressionOrValue; // REQUIRED — Classifier Identifier — string — Format - [a-zA-Z0-9][a-zA-Z0-9._~-]{1,63}. Unique classifier name.
      pages?: ExpressionOrValue; // optional — Pages — string — The page selection for multi-page PDF and TIFF documents, to extract information from individual pages and a range of pa
      locale?: ExpressionOrValue; // optional — Locale — string — Locale hint for text recognition and document analysis. Value may contain only the language code (ex. "en", "fr") or BCP
      stringIndexType?: ExpressionOrValue; // optional — String Index Type — string — Method used to compute string offset and length. Supported options: textElements, unicodeCodePoint, utf16CodeUnit.
      inputFileContent?: ExpressionOrValue; // optional — Document/Image File Content — binary — A PDF document or image (JPG, PNG, BMP, TIFF) file to analyze.
      inputFileUrl?: ExpressionOrValue; // optional — Document/Image URL — string — Url path for input file. Alternative to Document/Image File Content.
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "ClassifyDocumentV31",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Classify document with document classifier (v4.x API)
   * @returns Status and result of the analyze operation. - Body - AnalyzeResultOperation_V30
   * Operation id: `ClassifyDocumentV40`
   */
  classifyDocumentV40(
    parameters: {
      "api-version"?: ExpressionOrValue; // REQUIRED — Document Intelligence API version — string — Document Intelligence API version.
      classifierId?: ExpressionOrValue; // REQUIRED — Classifier Identifier — string — Format - [a-zA-Z0-9][a-zA-Z0-9._~-]{1,63}. Unique classifier name.
      inputFileContent?: ExpressionOrValue; // optional — Document/Image File Content — binary — A PDF document or image (JPG, PNG, BMP, TIFF) file to analyze.
      inputFileUrl?: ExpressionOrValue; // optional — Document/Image URL — string — Url path for input file. Alternative to Document/Image File Content.
      split?: ExpressionOrValue; // optional — Split Mode — string — File splitting behavior.
      pages?: ExpressionOrValue; // optional — Pages — string — The page selection for multi-page PDF and TIFF documents, to extract information from individual pages and a range of pa
      locale?: ExpressionOrValue; // optional — Locale — string — Locale hint for text recognition and document analysis. Value may contain only the language code (ex. "en", "fr") or BCP
      stringIndexType?: ExpressionOrValue; // optional — String Index Type — string — Method used to compute string offset and length. Supported options: textElements, unicodeCodePoint, utf16CodeUnit.
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "ClassifyDocumentV40",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },
} as const;
