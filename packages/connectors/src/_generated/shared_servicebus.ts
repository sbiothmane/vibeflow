// AUTO-GENERATED by packages/connectors/scripts/generate.ts.
// Do not edit by hand. Re-run `bun --filter @vibeflow/connectors generate`.
// Connector: shared_servicebus — Service Bus
// Source: https://learn.microsoft.com/en-us/connectors/servicebus/?accept=text/markdown

import { buildConnectorCall, type ActionSpec, type ConnectorCallOptions, type ExpressionOrValue, type TriggerSpec } from "../_runtime.js";

const API_ID = "/providers/Microsoft.PowerApps/apis/shared_servicebus";
const DEFAULT_CONNECTION_NAME = "shared_servicebus";

export const ServiceBus = {
  /** Connector id (`shared_servicebus`). */
  connectorId: "shared_servicebus",
  /** Power Automate apiId for this connector. */
  apiId: API_ID,

  /**
   * Abandon the message in a queue
   * Operation id: `AbandonMessageInQueue`
   */
  abandonMessageInQueue(
    parameters: {
      queueName?: ExpressionOrValue; // REQUIRED — Queue name — string — Name of the queue
      lockToken?: ExpressionOrValue; // REQUIRED — Lock token of the message — string — Lock token of the message to abandon
      queueType?: ExpressionOrValue; // optional — Queue type — string — Queue type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "AbandonMessageInQueue",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Abandon the message in a topic subscription
   * Operation id: `AbandonMessageInTopic`
   */
  abandonMessageInTopic(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      lockToken?: ExpressionOrValue; // REQUIRED — Lock token of the message — string — Lock token of the message to abandon
      subscriptionType?: ExpressionOrValue; // optional — Subscription type — string — Subscription type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "AbandonMessageInTopic",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Close a session in a queue
   * Operation id: `CloseSessionInQueue`
   */
  closeSessionInQueue(
    parameters: {
      queueName?: ExpressionOrValue; // REQUIRED — Queue name — string — Name of a queue
      sessionId?: ExpressionOrValue; // REQUIRED — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "CloseSessionInQueue",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Close a session in the topic
   * Operation id: `CloseSessionInTopic`
   */
  closeSessionInTopic(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      sessionId?: ExpressionOrValue; // REQUIRED — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "CloseSessionInTopic",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Complete the message in a queue
   * Operation id: `CompleteMessageInQueue`
   */
  completeMessageInQueue(
    parameters: {
      queueName?: ExpressionOrValue; // REQUIRED — Queue name — string — Name of the queue
      lockToken?: ExpressionOrValue; // REQUIRED — Lock token of the message — string — Lock token of the message to complete
      queueType?: ExpressionOrValue; // optional — Queue type — string — Queue type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "CompleteMessageInQueue",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Complete the message in a topic subscription
   * Operation id: `CompleteMessageInTopic`
   */
  completeMessageInTopic(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      lockToken?: ExpressionOrValue; // REQUIRED — Lock token of the message — string — Lock token of the message to complete
      subscriptionType?: ExpressionOrValue; // optional — Subscription type — string — Subscription type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "CompleteMessageInTopic",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Create a topic subscription
   * @returns The topic subscription. - Body - Subscription
   * Operation id: `CreateTopicSubscription`
   */
  createTopicSubscription(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      subscriptionFilter?: ExpressionOrValue; // REQUIRED — Filter — dynamic — Subscription filter
      subscriptionFilterType?: ExpressionOrValue; // optional — Filter type — string — Filter type
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "CreateTopicSubscription",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Dead-letter the message in a queue
   * Operation id: `DeadLetterMessageInQueue`
   */
  deadLetterMessageInQueue(
    parameters: {
      queueName?: ExpressionOrValue; // REQUIRED — Queue name — string — Name of the queue
      lockToken?: ExpressionOrValue; // REQUIRED — Lock token of the message — string — Lock token of the message to dead-letter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id
      deadLetterReason?: ExpressionOrValue; // optional — Dead letter reason — string — Dead letter reason
      deadLetterErrorDescription?: ExpressionOrValue; // optional — Dead letter error description — string — Dead letter error description
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "DeadLetterMessageInQueue",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Dead-letter the message in a topic subscription
   * Operation id: `DeadLetterMessageInTopic`
   */
  deadLetterMessageInTopic(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      lockToken?: ExpressionOrValue; // REQUIRED — Lock token of the message — string — Lock token of the message to dead-letter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id
      deadLetterReason?: ExpressionOrValue; // optional — Dead letter reason — string — Dead letter reason
      deadLetterErrorDescription?: ExpressionOrValue; // optional — Dead letter error description — string — Dead letter error description
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "DeadLetterMessageInTopic",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Defer the message in a queue
   * Operation id: `DeferMessageInQueue`
   */
  deferMessageInQueue(
    parameters: {
      queueName?: ExpressionOrValue; // REQUIRED — Queue name — string — Name of the queue
      lockToken?: ExpressionOrValue; // REQUIRED — Lock token of the message — string — Lock token of the message to defer
      queueType?: ExpressionOrValue; // optional — Queue type — string — Queue type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "DeferMessageInQueue",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Defer the message in a topic subscription
   * Operation id: `DeferMessageInTopic`
   */
  deferMessageInTopic(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      lockToken?: ExpressionOrValue; // REQUIRED — Lock token of the message — string — Lock token of the message to defer
      subscriptionType?: ExpressionOrValue; // optional — Subscription type — string — Subscription type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "DeferMessageInTopic",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Delete a topic subscription
   * Operation id: `DeleteTopicSubscription`
   */
  deleteTopicSubscription(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "DeleteTopicSubscription",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Get deferred message from a queue
   * @returns Service Bus Message - Body - ServiceBusMessage
   * Operation id: `GetDeferredMessageFromQueue`
   */
  getDeferredMessageFromQueue(
    parameters: {
      queueName?: ExpressionOrValue; // REQUIRED — Queue name — string — Name of the queue
      sequenceNumber?: ExpressionOrValue; // REQUIRED — Sequence number of message — integer — Sequence number of message
      queueType?: ExpressionOrValue; // optional — Queue type — string — Queue type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetDeferredMessageFromQueue",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Get deferred message from a topic subscription
   * @returns Service Bus Message - Body - ServiceBusMessage
   * Operation id: `GetDeferredMessageFromTopic`
   */
  getDeferredMessageFromTopic(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      sequenceNumber?: ExpressionOrValue; // REQUIRED — Sequence number of message — integer — Sequence number of message
      subscriptionType?: ExpressionOrValue; // optional — Subscription type — string — Queue type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetDeferredMessageFromTopic",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * When a message is received in a queue (auto-complete)
   * @remarks This is a trigger operation; pass it to flowBuilder.setTrigger().
   * @returns Service Bus Message - Body - ServiceBusMessage
   * Operation id: `GetMessageFromQueue`
   */
  getMessageFromQueue(
    parameters: {
      queueName?: ExpressionOrValue; // REQUIRED — Queue name — string — Name of the queue
      queueType?: ExpressionOrValue; // optional — Queue type — string — Queue type - Main or DeadLetter
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): TriggerSpec {
    return buildConnectorCall({
      kind: "trigger",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetMessageFromQueue",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as TriggerSpec;
  },

  /**
   * When a message is received in a topic subscription (auto-complete)
   * @remarks This is a trigger operation; pass it to flowBuilder.setTrigger().
   * @returns Service Bus Message - Body - ServiceBusMessage
   * Operation id: `GetMessageFromTopic`
   */
  getMessageFromTopic(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      subscriptionType?: ExpressionOrValue; // optional — Subscription type — string — Subscription type - Main or DeadLetter
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): TriggerSpec {
    return buildConnectorCall({
      kind: "trigger",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetMessageFromTopic",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as TriggerSpec;
  },

  /**
   * When one or more messages arrive in a queue (auto-complete)
   * @remarks This is a trigger operation; pass it to flowBuilder.setTrigger().
   * @returns Service Bus Message - Body - ServiceBusMessage
   * Operation id: `GetMessagesFromQueue`
   */
  getMessagesFromQueue(
    parameters: {
      queueName?: ExpressionOrValue; // REQUIRED — Queue name — string — Name of the queue
      maxMessageCount?: ExpressionOrValue; // optional — Maximum message count — integer — The maximum number of messages to return in the batch
      queueType?: ExpressionOrValue; // optional — Queue type — string — Queue type - Main or DeadLetter
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): TriggerSpec {
    return buildConnectorCall({
      kind: "trigger",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetMessagesFromQueue",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as TriggerSpec;
  },

  /**
   * Get messages from a queue (peek-lock)
   * @returns - response - array of ServiceBusMessage
   * Operation id: `GetMessagesFromQueueWithPeekLock`
   */
  getMessagesFromQueueWithPeekLock(
    parameters: {
      queueName?: ExpressionOrValue; // REQUIRED — Queue name — string — Name of the queue
      maxMessageCount?: ExpressionOrValue; // optional — Maximum message count — integer — The maximum number of messages to return in the batch
      queueType?: ExpressionOrValue; // optional — Queue type — string — Queue type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetMessagesFromQueueWithPeekLock",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * When one or more messages arrive in a topic (auto-complete)
   * @remarks This is a trigger operation; pass it to flowBuilder.setTrigger().
   * @returns Service Bus Message - Body - ServiceBusMessage
   * Operation id: `GetMessagesFromTopic`
   */
  getMessagesFromTopic(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      maxMessageCount?: ExpressionOrValue; // optional — Maximum message count — integer — The maximum number of messages to return in the batch
      subscriptionType?: ExpressionOrValue; // optional — Subscription type — string — Subscription type - Main or DeadLetter
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): TriggerSpec {
    return buildConnectorCall({
      kind: "trigger",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetMessagesFromTopic",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as TriggerSpec;
  },

  /**
   * Get messages from a topic subscription (peek-lock)
   * @returns - response - array of ServiceBusMessage
   * Operation id: `GetMessagesFromTopicWithPeekLock`
   */
  getMessagesFromTopicWithPeekLock(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      maxMessageCount?: ExpressionOrValue; // optional — Maximum message count — integer — The maximum number of messages to return in the batch
      subscriptionType?: ExpressionOrValue; // optional — Subscription type — string — Subscription type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetMessagesFromTopicWithPeekLock",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * When a message is received in a queue (peek-lock)
   * @remarks This is a trigger operation; pass it to flowBuilder.setTrigger().
   * @returns Service Bus Message - Body - ServiceBusMessage
   * Operation id: `GetNewMessageFromQueueWithPeekLock`
   */
  getNewMessageFromQueueWithPeekLock(
    parameters: {
      queueName?: ExpressionOrValue; // REQUIRED — Queue name — string — Name of the queue
      queueType?: ExpressionOrValue; // optional — Queue type — string — Queue type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id - None or Next Available or provide session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): TriggerSpec {
    return buildConnectorCall({
      kind: "trigger",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetNewMessageFromQueueWithPeekLock",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as TriggerSpec;
  },

  /**
   * When a message is received in a topic subscription (peek-lock)
   * @remarks This is a trigger operation; pass it to flowBuilder.setTrigger().
   * @returns Service Bus Message - Body - ServiceBusMessage
   * Operation id: `GetNewMessageFromTopicWithPeekLock`
   */
  getNewMessageFromTopicWithPeekLock(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      subscriptionType?: ExpressionOrValue; // optional — Subscription type — string — Subscription type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id - None or Next Available or provide session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): TriggerSpec {
    return buildConnectorCall({
      kind: "trigger",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetNewMessageFromTopicWithPeekLock",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as TriggerSpec;
  },

  /**
   * When one or more messages arrive in a queue (peek-lock)
   * @remarks This is a trigger operation; pass it to flowBuilder.setTrigger().
   * @returns Service Bus Message - Body - ServiceBusMessage
   * Operation id: `GetNewMessagesFromQueueWithPeekLock`
   */
  getNewMessagesFromQueueWithPeekLock(
    parameters: {
      queueName?: ExpressionOrValue; // REQUIRED — Queue name — string — Name of the queue
      maxMessageCount?: ExpressionOrValue; // optional — Maximum message count — integer — The maximum number of messages to return in the batch
      queueType?: ExpressionOrValue; // optional — Queue type — string — Queue type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id - None or Next Available or provide session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): TriggerSpec {
    return buildConnectorCall({
      kind: "trigger",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetNewMessagesFromQueueWithPeekLock",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as TriggerSpec;
  },

  /**
   * When one or more messages arrive in a topic (peek-lock)
   * @remarks This is a trigger operation; pass it to flowBuilder.setTrigger().
   * @returns Service Bus Message - Body - ServiceBusMessage
   * Operation id: `GetNewMessagesFromTopicWithPeekLock`
   */
  getNewMessagesFromTopicWithPeekLock(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      maxMessageCount?: ExpressionOrValue; // optional — Maximum message count — integer — The maximum number of messages to return in the batch
      subscriptionType?: ExpressionOrValue; // optional — Subscription type — string — Subscription type - Main or DeadLetter
      sessionId?: ExpressionOrValue; // optional — Session id — string — Session id - None or Next Available or provide session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): TriggerSpec {
    return buildConnectorCall({
      kind: "trigger",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetNewMessagesFromTopicWithPeekLock",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as TriggerSpec;
  },

  /**
   * Renew lock on the message in a queue
   * Operation id: `RenewLockOnMessageInQueue`
   */
  renewLockOnMessageInQueue(
    parameters: {
      queueName?: ExpressionOrValue; // REQUIRED — Queue name — string — Name of the queue
      lockToken?: ExpressionOrValue; // REQUIRED — Lock token of the message — string — Lock token of the message to renew lock
      queueType?: ExpressionOrValue; // optional — Queue type — string — Queue type - Main or DeadLetter
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "RenewLockOnMessageInQueue",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Renew lock on the message in a topic subscription
   * Operation id: `RenewLockOnMessageInTopic`
   */
  renewLockOnMessageInTopic(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      lockToken?: ExpressionOrValue; // REQUIRED — Lock token of the message — string — Lock token of the message to renew lock
      subscriptionType?: ExpressionOrValue; // optional — Subscription type — string — Subscription type - Main or DeadLetter
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "RenewLockOnMessageInTopic",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Renew lock on the session in a queue
   * Operation id: `RenewLockOnSessionInQueue`
   */
  renewLockOnSessionInQueue(
    parameters: {
      queueName?: ExpressionOrValue; // REQUIRED — Queue name — string — Name of the queue
      sessionId?: ExpressionOrValue; // REQUIRED — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "RenewLockOnSessionInQueue",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Renew lock on the session in a topic subscription
   * Operation id: `RenewLockOnSessionInTopic`
   */
  renewLockOnSessionInTopic(
    parameters: {
      topicName?: ExpressionOrValue; // REQUIRED — Topic name — string — Name of the topic
      subscriptionName?: ExpressionOrValue; // REQUIRED — Topic subscription name — string — Name of the topic subscription
      sessionId?: ExpressionOrValue; // REQUIRED — Session id — string — Session id
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "RenewLockOnSessionInTopic",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Send message
   * Operation id: `SendMessage`
   */
  sendMessage(
    parameters: {
      entityName?: ExpressionOrValue; // REQUIRED — Queue/Topic name — string — Name of the queue or topic
      ContentData?: ExpressionOrValue; // optional — Content — byte — Content of the message
      ContentType?: ExpressionOrValue; // optional — Content Type — string — Content type of the message content
      Properties?: ExpressionOrValue; // optional — object — Key-value pairs for each brokered property
      MessageId?: ExpressionOrValue; // optional — Message Id — string — This is a user-defined value that Service Bus can use to identify duplicate messages, if enabled.
      To?: ExpressionOrValue; // optional — string — Send to address
      ReplyTo?: ExpressionOrValue; // optional — Reply To — string — Address of the queue to reply to
      ReplyToSessionId?: ExpressionOrValue; // optional — Reply To Session Id — string — Identifier of the session to reply to
      Label?: ExpressionOrValue; // optional — string — Application specific label
      ScheduledEnqueueTimeUtc?: ExpressionOrValue; // optional — date-time — Date and time, in UTC, when the message will be added to the queue
      SessionId?: ExpressionOrValue; // optional — Session Id — string — Identifier of the session
      CorrelationId?: ExpressionOrValue; // optional — Correlation Id — string — Identifier of the correlation
      SequenceNumber?: ExpressionOrValue; // optional — Sequence Number — integer — Identifier of the sequence number
      LockToken?: ExpressionOrValue; // optional — Lock Token — string — The lock token of the message as a string.
      TimeToLive?: ExpressionOrValue; // optional — Time To Live — integer — This is the duration, in ticks, that a message is valid. The duration starts from when the message is sent to the Servic
      systemProperties?: ExpressionOrValue; // optional — System properties — string — System properties - None or Run Details. Run Details will add run metadata property details as custom properties in the 
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "SendMessage",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Send one or more messages
   * Operation id: `SendMessages`
   */
  sendMessages(
    parameters: {
      entityName?: ExpressionOrValue; // REQUIRED — Queue/Topic name — string — Name of the queue or topic
      ContentData?: ExpressionOrValue; // optional — Content — byte — Content of the message
      ContentType?: ExpressionOrValue; // optional — Content Type — string — Content type of the message content
      Properties?: ExpressionOrValue; // optional — object — Key-value pairs for each brokered property
      MessageId?: ExpressionOrValue; // optional — Message Id — string — This is a user-defined value that Service Bus can use to identify duplicate messages, if enabled.
      To?: ExpressionOrValue; // optional — string — Send to address
      ReplyTo?: ExpressionOrValue; // optional — Reply To — string — Address of the queue to reply to
      ReplyToSessionId?: ExpressionOrValue; // optional — Reply To Session Id — string — Identifier of the session to reply to
      Label?: ExpressionOrValue; // optional — string — Application specific label
      ScheduledEnqueueTimeUtc?: ExpressionOrValue; // optional — date-time — Date and time, in UTC, when the message will be added to the queue
      SessionId?: ExpressionOrValue; // optional — Session Id — string — Identifier of the session
      CorrelationId?: ExpressionOrValue; // optional — Correlation Id — string — Identifier of the correlation
      SequenceNumber?: ExpressionOrValue; // optional — Sequence Number — integer — Identifier of the sequence number
      LockToken?: ExpressionOrValue; // optional — Lock Token — string — The lock token of the message as a string.
      TimeToLive?: ExpressionOrValue; // optional — Time To Live — integer — This is the duration, in ticks, that a message is valid. The duration starts from when the message is sent to the Servic
      systemProperties?: ExpressionOrValue; // optional — System properties — string — System properties - None or Run Details. Run Details will add run metadata property details as custom properties in the 
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "SendMessages",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },
} as const;
