// AUTO-GENERATED by packages/connectors/scripts/generate.ts.
// Do not edit by hand. Re-run `bun --filter @vibeflow/connectors generate`.
// Connector: shared_azureautomation — Azure Automation
// Source: https://learn.microsoft.com/en-us/connectors/azureautomation/?accept=text/markdown

import { buildConnectorCall, type ActionSpec, type ConnectorCallOptions, type ExpressionOrValue, type TriggerSpec } from "../_runtime.js";

const API_ID = "/providers/Microsoft.PowerApps/apis/shared_azureautomation";
const DEFAULT_CONNECTION_NAME = "shared_azureautomation";

export const AzureAutomation = {
  /** Connector id (`shared_azureautomation`). */
  connectorId: "shared_azureautomation",
  /** Power Automate apiId for this connector. */
  apiId: API_ID,

  /**
   * Create job
   * @returns - Body - CreateJobResponse
   * Operation id: `CreateJob`
   */
  createJob(
    parameters: {
      subscriptionId?: ExpressionOrValue; // REQUIRED — Subscription — string — The unique identifier for the Microsoft Azure subscription. The subscription ID forms part of the ID for every Azure res
      resourceGroupName?: ExpressionOrValue; // REQUIRED — Resource Group — string — The name of the Azure Resource Group.
      automationAccount?: ExpressionOrValue; // REQUIRED — Automation Account — string — The name of the Azure Automation Account.
      body?: ExpressionOrValue; // REQUIRED — dynamic
      runbookName?: ExpressionOrValue; // optional — Runbook Name — string — Name of the runbook to run.
      wait?: ExpressionOrValue; // optional — Wait for Job — boolean — Wait for the job to finish before completing the action.
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "CreateJob",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Get job output
   * @returns Runbook content from the job - Content - binary
   * Operation id: `GetJobOutput`
   */
  getJobOutput(
    parameters: {
      subscriptionId?: ExpressionOrValue; // REQUIRED — Subscription — string — The unique identifier for the Microsoft Azure subscription. The subscription ID forms part of the ID for every Azure res
      resourceGroupName?: ExpressionOrValue; // REQUIRED — Resource Group — string — The name of the Azure Resource Group.
      automationAccount?: ExpressionOrValue; // REQUIRED — Automation Account — string — The name of the Azure Automation Account.
      jobId?: ExpressionOrValue; // REQUIRED — Job ID — string — GUID for the ID of the run.
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetJobOutput",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },

  /**
   * Get status of job
   * @returns - Body - CreateJobResponse
   * Operation id: `GetStatusOfJob`
   */
  getStatusOfJob(
    parameters: {
      subscriptionId?: ExpressionOrValue; // REQUIRED — Subscription — string — The unique identifier for the Microsoft Azure subscription. The subscription ID forms part of the ID for every Azure res
      resourceGroupName?: ExpressionOrValue; // REQUIRED — Resource Group — string — The name of the Azure Resource Group.
      automationAccount?: ExpressionOrValue; // REQUIRED — Automation Account — string — The name of the Azure Automation Account.
      jobId?: ExpressionOrValue; // REQUIRED — Job ID — string — GUID for the ID of the run.
    } & Record<string, ExpressionOrValue>,
    options?: ConnectorCallOptions,
  ): ActionSpec {
    return buildConnectorCall({
      kind: "action",
      apiId: API_ID,
      defaultConnectionName: DEFAULT_CONNECTION_NAME,
      operationId: "GetStatusOfJob",
      parameters: parameters as Record<string, ExpressionOrValue>,
      options,
    }) as ActionSpec;
  },
} as const;
